# `padosoft/laravel-flow-admin` — PROGRESS

> Durable handoff log. Read this first when restarting a session.
> Per-PR Copilot/CI history lives on the PR; only durable restart state lands here.

## Now / Next / Blocked

- **2026-07-18 — 🏁 v2.0.0 RELEASED — Flow 2.0 PROGRAM COMPLETE.** `padosoft/laravel-flow-admin` **v2.0.0** (Flow Studio) is tagged + GitHub-released + on Packagist, requiring core `padosoft/laravel-flow` **v2.0.0** and (optional) `padosoft/laravel-flow-ai` **v1.0.0**. Gate G3 (PR #43) → knowhow consolidation (AGENTS.md, PR #45) → the 409-vs-503 admin fix (PR #44) → composer conversion `dev-main`→`^2.0`/`^1.0` + path repos removed + dead CI sibling-checkouts dropped (PR #46) → tag `v2.0.0`. Install: `composer require padosoft/laravel-flow-admin`. **Nothing remains on the roadmap.**
  - **Post-release quality fixes (this PR, → `[Unreleased]`)**: (1) E2E CI flake ROOT-CAUSED and FIXED — `serve-testbench.mjs` sets `PHP_CLI_SERVER_WORKERS=4` so `artisan serve`'s `php -S` handles requests concurrently (no more single-threaded stall/cascade). (2) The flaky KPI window-boundary test is now DETERMINISTIC — `EloquentReadModel` takes an injectable `?Closure $clock`; the test injects a fixed instant (verified 20× green). Both were pre-existing, documented in LESSON, now closed.

- **2026-07-18 — E-PR6 (working mutations) — ✅ MERGED into `task/v2e-studio`** (PR #42, squash `169691f`, branch deleted). Full DoD: local gates → local Copilot review to NO_FINDINGS → **6 PR-review rounds** (7+2+1+2+1+1 comments, all addressed) → 0 outstanding + all 6 CI code checks green + `MERGEABLE`/`CLEAN`. Review-fix highlights: confirm gates on Replay/Redeliver; `toast()` XSS→`textContent`; `{tokenHash}` route `[A-Fa-f0-9]{64}` + array fallback sha1→sha256; outbox `{id}` route `[0-9]{1,18}` + controller round-trip guard; `FlowMutation` fail-closed on a malformed payload (loosened the callable phpdoc so the guard isn't PHPStan-dead); `canRedeliver`/`canDecide` aligned to the route constraints. Final **228 tests / 1042 assertions**. **ALL Macro E subtasks COMPLETE → next: Gate G3 (merge `task/v2e-studio` → `main`) → Macro G (release).** NOTE: the E2E matrix flaked a different single browser on most pushes (single-threaded `testbench serve` stall — re-run the failed job, not a code change; LESSON 2026-07-17).

- **2026-07-17 — E-PR6 (working mutations) — (superseded by the merge note above).** The 4 core `@api` seams merged (core PRs #93–#96), so this is UNBLOCKED; `composer update` pulled them into the admin vendor.
  - **Delivered — 5 mutation endpoints** returning the uniform `{success,message,data}` JSON contract, each wrapped in `Support\Authorize::action(...)` (deny-by-default): `POST /approvals/{tokenHash}/approve|reject` (`ApprovalsController` → `Flow::resumeByHash()`/`rejectByHash()`), `POST /runs/{id}/cancel|replay` (`RunDetailController` → `Flow::cancel()`/`replay()`), `POST /outbox/{id}/redeliver` (`OutboxController` → `Flow::redeliverWebhook()`, cast id `(int)`). Controllers inject core `FlowEngine` (first engine use in the admin). Shared `Support\FlowMutation::run()` maps engine exceptions → HTTP: `FlowInputException`→422, `ApprovalPersistenceException`→503 (caught before its parent), `FlowExecutionException`→409 (curated message surfaced — only ids already in the URL), unknown `Throwable`→sanitized 500 (class-only log). `redeliverWebhook` returns **bool** → false re-thrown as a 409.
  - **GAP CLOSED**: `tokenHash` was dropped at the adapter boundary — threaded core `Dashboard\ApprovalSummary::$tokenHash` → admin `Contracts\Dto\ApprovalSummary` → both `mapApproval()` adapters → `ViewModels\ApprovalCard` (+ `canDecide()`) → approvals blade. Added `OutboxRow::$canRedeliver` (= `status==='failed'`, narrower than `canRetry`) and `RunDetailViewModel::canCancel()`/`canReplay()` (active vs terminal, allowlist on public slug).
  - **UI**: wired the 2 inert approvals buttons; ADDED an Actions column + Redeliver on outbox; ADDED Cancel/Replay on run detail. All via a shared `partials/action-runner.blade.php` vanilla-JS enhancer on `[data-flow-action]` (csrf-meta header, `data-confirm` for destructive reject/cancel, reuses `window.flowAdminToast` — now exposed from the layout IIFE, `location.reload()` on success).
  - **Local gates GREEN (Herd PHP 8.5)**: Pint ✅, PHPStan 8 ✅, PHPUnit ✅ **228 tests / 1042 assertions** (Feature: approve/reject/cancel/replay/redeliver 403-by-default + real-engine round-trips via `MigratesFlowTables` + persistence + file lock store + `$engine->execute()` on an approval-gated flow; Unit: `FlowMutation` mapping + all 4 eligibility flags), ESLint ✅, Vite build ✅, Playwright ✅ **99 runs / 6 skipped** (33 scenarios × 3; 5 new mutation scenarios pass on all 3 browsers). README (feature bullets + routes + counts) + LESSON synced. Fixed a Playwright strict-mode violation (`.muted` matched destination + placeholder → dedicated `data-testid="outbox-no-action"`).
  - **Next**: local Copilot CLI review loop → NO_FINDINGS → push → PR to `task/v2e-studio` → Copilot + CI → merge. Then **Gate G3 (merge `task/v2e-studio` → `main`) → Macro G (release)**.

- **2026-07-17 — E-PR8b (Advisor suggestions inbox) — ✅ MERGED into `task/v2e-studio`** (PR #41, squash `ddee458`, branch deleted). Full DoD: local gates → 3-round local Copilot review to NO_FINDINGS → 2 PR-review rounds (depth cap / unique test-ids / assert-by-label) to 0 outstanding + all 6 CI green. **E-PR8 (AI builder + advisor) is now FULLY COMPLETE.** Baseline **180 tests / 945 assertions**, 84 E2E runs / 6 skipped. **Macro E remaining: E-PR6 (working mutations, BLOCKED on 4 core `@api` seams) → Gate G3 (merge `task/v2e-studio` → `main`) → Macro G (release).**

- **2026-07-17 — E-PR8b (Advisor suggestions inbox) — (superseded by the merge note above).** Branch `task/v2e-08b-advisor-inbox` (off `task/v2e-studio`, which now has E-PR8a merged). Completes E-PR8.
  - **Delivered**: `AdvisorController` (`index` renders the inbox page; `scan` = gated POST). `POST /advisor/scan` — `edit_definition`-gated (empty flowName → the shipped Deny/Allow authorizers ignore it; a per-flow gate isn't possible since `suggest()` is all-flows), `class_exists(FlowAdvisor)` 404 INSIDE the gate (uniform 403, route registered unconditionally — same no-oracle posture as ai-build), `throttle:6,1,flow-admin-advisor-scan` (a scan reads run history and WRITES draft versions), calls `app(FlowAdvisor::class)->suggest()`, returns `list<{flow, draft_version, draft_id, finding:{type,summary,rationale}}>` with `boundRationale()`/summary caps; sanitized 500 (class-only log) on throw. **Advisor SIDE EFFECT is intentional**: each finding persists a NEW draft version (advisor rationale in `metadata['advisor']`), never published — the operator reviews/publishes via E-PR4's Versions page (the inbox card deep-links there). `GET /advisor` Blade page + sidebar nav (`activity` icon) + a lean **vanilla-JS** island (no React — button → POST → cards, pure-DOM render, loading/empty/error states per rule-admin-ajax-pattern). Advisor CSS in `admin.css`.
  - **Gotcha fixed**: `FlowAdvisor` CANNOT auto-wire (ctor takes `array $analyzers`/`array $exposedFlowNames`/`int $sampleSize`), so `app(FlowAdvisor::class)` 500'd with `BindingResolutionException` in the served E2E app — the AI pack's provider (which binds it) wasn't loaded there. Fix: register `Padosoft\LaravelFlowAI\LaravelFlowAIServiceProvider` explicitly in `testbench.yaml` (a real consumer app gets it via package auto-discovery; the FakeLlmClient override in admin `boot()` still wins over the now-registered AI provider's `LlmClient` — verified: all 27 chromium E2E green). See LESSON 2026-07-17.
  - **Local gates GREEN (Herd PHP 8.5; CI 8.3/8.4)**: Pint ✅, PHPStan 8 ✅, PHPUnit ✅ **180 tests / 945 assertions** (5 new: advisor routes registered, index renders, scan 403-by-default, scan returns suggestions + creates draft without publishing (canned `Analyzer` double + shipped `AllowAllAuthorizer`), scan sanitized-500 on throw), ESLint ✅, Vite build ✅, Playwright ✅ **84 runs / 6 skipped** (28 scenarios × 3; 2 new advisor scenarios pass on all 3 browsers). README (feature bullet + comparison + routes + counts) + AGENTS.md AI-surface note synced.
  - **Local Copilot CLI review round 1 → 6 findings, all addressed** (no must-fix; XSS/auth/CSRF explicitly cleared): (1) documented the empty-`$flowName` = all-flows-scan semantic on `ActionAuthorizer::canEditDefinition()` + AGENTS.md; (2) replaced the `versionsUrlFor` `__FLOW__` string-replace hack with a PHP-split prefix/suffix template; (3) added a `window.confirm` before the mass-mutating scan (E2E accepts the dialog); (4) flattened `.advisor-rationale` to a left-accent (no nested-card border); (5) documented that the `class_exists` 404-absent branch is defensive + not unit-testable (pack is require-dev, always present in CI); (6) added a depth cap (4) to `boundValue` so a pathological third-party `Analyzer` rationale can't exhaust the stack. Gates re-green (180/945, advisor E2E 2/2).
  - **Next remote step**: re-run local Copilot review on the final diff → push → PR `task/v2e-08b-advisor-inbox` → `task/v2e-studio` → Copilot + CI → merge. Then E-PR6 (blocked on core seams), Gate G3, Macro G.

- **2026-07-17 — E-PR8a (AI Flow Builder UI) — ✅ MERGED into `task/v2e-studio`** (PR #40, squash `16ea499`, branch deleted). Full DoD loop: local gates green → 4-round local Copilot CLI review to NO_FINDINGS → 8 PR-review rounds (each a single minor finding: optional-dep posture, throttle bucket isolation, drop client model, bounded violations, auth-before-detection, whitespace-prompt, doc consistency, modal a11y — Escape/aria) to **0 outstanding comments + all 6 CI checks green**. Baseline now **175 tests / 921 assertions**, 72 E2E / 6 skipped. **Next: E-PR8b (advisor suggestions inbox)** — `FlowAdvisor::suggest()/improve()` (deterministic, no LLM), then E-PR6 (blocked on core seams), Gate G3, Macro G.
  - **E-PR8b design (verified against `laravel-flow-ai` src 2026-07-17)**: `FlowAdvisor` (`@api`) is container-resolvable from the AI provider's `register()` (deps: `FlowDashboardReadModel` + `DefinitionRepository` + 4 deterministic analyzers + optional `PayloadRedactor`; **NO LlmClient** → no fake needed, works whenever the AI pack is installed). **Critical**: `suggest()`/`improve($name)` have a SIDE EFFECT — each call with findings persists a NEW draft `GraphDefinition` version via `DefinitionRepository::createDraft()` (no dedup), carrying the finding rationale in `metadata['advisor']` (`generated_at`, `based_on_version`, `findings[]`). So the inbox is a **gated POST scan action** (creates drafts), NOT a pure GET. Returns `list<Suggestion{definitionName, draftVersion:int, finding:Finding{type,summary,rationale:array}}>`. **MVP plan**: (1) `POST /advisor/scan` gated `edit_definition` (+ throttle — it can create many drafts and scans run history), `class_exists(FlowAdvisor::class)`-guarded INSIDE the auth gate (uniform 403; route registered unconditionally — same no-oracle posture as ai-build), calls `app(FlowAdvisor::class)->suggest()`, returns the suggestions (findings + the draft version each was written onto). Sanitize: rationale is already redactor-passed by the advisor, but bound length/count like `boundViolations`. (2) `GET /advisor` React island page + sidebar nav entry: a "Scan for suggestions" button (explicit — it mutates) → cards grouped by flow, each showing finding `type`/`summary`/rationale + a **link into E-PR4's versions page** (`/studio/{name}/versions`) with the created draft pre-selected to diff against `based_on_version`. (3) Accept = publish the draft via the EXISTING versions/publish UI (no new endpoint); Dismiss = leave the draft unpublished (a future archive control can prune — note this as a known gap). (4) PHPUnit: scan 403-by-default, scan-with-allowing-authorizer returns suggestions + creates draft rows (seed a flow with failing run history so `FailureHotspotAnalyzer` fires), 404 when AI pack absent (inside auth). (5) Playwright: scan button → cards render → view-draft link. Branch `task/v2e-08b-advisor-inbox` off `task/v2e-studio`.
  - **Delivered**: `padosoft/laravel-flow-ai` added as an OPTIONAL dep — `require-dev` + `suggest` (NOT `require`; local-review finding 1: a hard `require` on an unpublished path-repo would break `composer require` downstream AND make the `class_exists`/`interface_exists` guards dead code) via a Composer `path` repo (`../laravel-flow-ai`, `symlink:false`); CI checks it out as a THIRD sibling (`padosoft/laravel-flow-ai`, ref `main`) in BOTH the `php` and `e2e` jobs (installed because CI runs `composer install/update` with dev deps). `src/Ai/FakeLlmClient.php` (dev/E2E-only `LlmClient` returning a canned single-node `demo.trigger` graph). Provider binds the fake in **boot()** (`bindFakeLlmClientIfOptedIn()`), gated by `config('flow-admin.ai.fake')` (env `FLOW_ADMIN_FAKE_LLM`) — overrides BOTH the global `LlmClient` singleton AND FlowBuilderService's **contextual** `LlmClient` binding (the AI pack scopes its own guarded/network client to that class via `when()->needs()->give()`, which a plain global override would miss); refuses in the production environment (Log::warning), same posture as `AllowAllAuthorizer`. Reads config not `env()` directly (larastan `noEnvCallsOutsideOfConfig`). Endpoint `POST /studio/{name}/ai-build` (`StudioController::aiBuild`) — gated `edit_definition`, validates `prompt` (3–4000); the model is config-only (`config('flow-admin.ai.model')`), never client-supplied; calls `FlowBuilderService::build()`, returns the serialized `GraphSerializer` envelope on success / 422 `{data.violations}` on `FlowBuilderResult::failed` / sanitized 500 (class-only log) if the client throws / 404 if the AI pack is absent (`class_exists` guard, also drives the view's `data-ai-build-url` omission). React: `AiBuildModal` (prompt textarea → Generate/Cancel, shows violations) + a **Build with AI** button in the editor save-bar; on success loads the envelope onto the canvas via `editableGraphToFlowElements` (REPLACES nodes/edges, bumps the dry-run stale guard) — **never persists**, operator saves via the existing `/draft`.
  - **Local gates GREEN (local runtime Herd PHP 8.5; CI matrix is PHP 8.3/8.4)**: Pint ✅, PHPStan level 8 ✅, PHPUnit ✅ **175 tests / 921 assertions** (6 new: ai-build 403-by-default, happy-path-no-persist, invalid-graph 422, prompt-validation 422, client-throws sanitized-500, rate-limit 429), ESLint ✅, Vite build ✅, Playwright ✅ **72 E2E passed / 6 skipped** (26 scenarios × 3; 2 new AI-build scenarios pass on all 3 browsers). README (feature bullet + comparison row + routes/authz + test counts) synced.
  - **Local Copilot CLI review round 1 → 4 findings, all fixed**: (1) AI dep moved `require`→`require-dev`+`suggest` (above); (2) `throttle:12,1` on the ai-build route (billable/external call — needs a working cache store, so tests + `testbench.yaml` force `CACHE_STORE=array` / `cache.default=array`, else the DB cache 500s with "no such table: cache"); (3) dropped the client-supplied `model` override — model is config-only (`flow-admin.ai.model`), an operator cost decision, not end-user-forceable; (4) `boundViolations()` caps the 422 `data.violations` (≤20 entries, ≤300 chars each) so a future guardrail-layer message can't dump leaky text to the browser. Re-ran gates green after fixes (174/905, throttle E2E still 2/2 on all browsers).
  - **Next remote step**: re-run the local Copilot CLI review on the final diff (post-fixes) → push → PR `task/v2e-08-advisor-ai` → `task/v2e-studio` → add Copilot reviewer → CI green + comments resolved → merge. Then E-PR8b (advisor inbox), then E-PR6 (blocked on core seams — see below), Gate G3, Macro G.

- **2026-07-17 — Macro E: E-PR3, E-PR4, E-PR5, E-PR7 all MERGED into `task/v2e-studio`** (heads `8a51243`, `7d0a2f1`, `4cd0c51`, `0f3e929`); the supporting **core PR #92** (`Dashboard\StepSummary::$cacheHit`, additive `@api`) is merged into `padosoft/laravel-flow` main. Each went through the full DoD loop (local gates → 3–8-round local Copilot CLI review to zero findings → PR → green CI → Copilot PR review resolved → merge). Current local baseline: **169 tests / 882 assertions**, **66 E2E passed / 6 skipped** (24 scenarios × 3).
  - **E-PR7 (dry-run, PR #39)**: a "Dry run" button on the Studio editor statically plans the current graph via core's `DryRunPlanner` (`@api`, added to the admin's public-surface allowlist in `AGENTS.md` + the admin-shell skill as the only permitted `Executor\DryRun\*` classes) — Kahn-wave execution order + per-node/total cost, zero writes/no handler; validated (`fromArray` + `GraphValidator`) → 422; sanitized-500 fallback; stale-response race guarded by a monotonic request id; a shared `buildGraphPayload()` now backs save + dry-run.
  - **Remaining: E-PR8 (advisor + AI builder UI) and E-PR6 (mutations), then Gate G3 + Macro G.** **E-PR8 is UNBLOCKED but the largest subtask** — `laravel-flow-ai` (on `main`) exposes `Builder\FlowBuilderService` (`@api`, `build(string $prompt, string $model): FlowBuilderResult`, depends on `Contracts\LlmClient` — bind a FAKE `LlmClient` for tests/E2E, no real network) and `Advisor\FlowAdvisor` (`@api`, `suggest(): array` / `improve(string $name): array`, deterministic — deps `FlowDashboardReadModel` + `DefinitionRepository` + analyzers, no LLM). Scope: (1) add `laravel-flow-ai` as a Composer `path` dep of the admin (like core) + update CI to checkout it as a THIRD sibling (`padosoft/laravel-flow-ai`) alongside core; (2) HTTP endpoints `POST /studio/ai-build` (prompt → `FlowBuilderService::build` → preview graph) and `GET /advisor/suggestions` + accept (→ draft version) / dismiss, all authz-gated; (3) React AI-Builder modal (prompt → preview canvas → Insert/Regenerate/Cancel) + suggestions inbox (new-flow vs improve cards, View-diff overlay reusing E-PR4's diff, Accept/Dismiss); (4) Feature tests (fake `LlmClient`; each mutation 403-by-default), Playwright happy paths. **E-PR6 stays BLOCKED** on the 4 core `@api` seams (approve/reject-by-hash, cancel-run, replay-run, retry-webhook-by-id) — see the detailed per-seam implementation notes below.
  - **E-PR4 (versioning, PR #37)**: publish w/ immutability modal + version list + **server-computed node diff** (added/removed/changed, redacted — no node `config` leaves the server).
  - **E-PR5 (live monitor, PR #38)**: `/runs/{id}/monitor` React island — Echo subscribe to core's private per-run channel (`{prefix}.run.{runId}`, events `node.transitioned`/`run.progress`) with **polling fallback** (`GET /runs/{id}/monitor-state`, node-state-only, no payloads); 9 real `NodeState` colors + ⚡ cache-hit badge; progress aligned to core `RunRollup` terminal buckets. Added `PackagedAssetController` catch-all (`/_flow-admin/assets/{file}`) because a 2nd React island makes Vite split the shared React runtime into a hashed chunk. Threaded `cacheHit` through `Contracts\Dto\Step` + both adapters. Local monitor broadcasting requires the host to authorize the channel in `routes/channels.php` (documented in README). Current local baseline: **166 tests / 865 assertions**, **63 E2E passed / 6 skipped** (23 scenarios × 3).
  - **Next: E-PR6 (working mutations) — BLOCKED on missing core `@api` seams (verified 2026-07-17).** The admin currently has NO mutation endpoints beyond `studio.draft`/`studio.publish`/`theme.toggle`; the README's "every mutation route…" text is aspirational. The **authorization scaffolding already exists** (`Support\Authorize` maps `approve`/`reject`/`cancel_run`/`replay_run`/`retry_webhook` to the `ActionAuthorizer`), but the CORE mutation seams the controllers must call are missing:
    - **Approvals**: every core approval mutation — `Flow::resume($plainToken,…)`, `Flow::reject($plainToken,…)`, `ApprovalTokenManager::approve/reject/approveForRunStatus/rejectForRunStatus` — takes the **PLAIN token**. The admin only holds token HASHES (plain tokens are never recoverable from storage, by design). So the dashboard **cannot** approve/reject with what it has. Needs a CORE `@api` addition: a hash/record-based approve/reject (e.g. `approveByHash(string $tokenHash, …)` or a `Dashboard`-scoped decision seam operating on `FlowApprovalRecord`).
    - **Cancel / replay / retry-run**: core exposes NO `cancel`/`replay`/`retry` methods on the `Flow` facade or any `@api` contract (grep confirms). Each needs a CORE `@api` seam.
    - **Webhook redeliver**: no public retry seam on the outbox repository surface; only `Console\DeliverWebhookOutboxCommand` (delivers ALL pending). Needs a CORE `@api` "redeliver one outbox row by id" seam.
    - **Correct sequence**: author + merge the core `@api` PRs (approve-by-hash, cancel-run, replay-run, retry-webhook-by-id) FIRST — each through core's own DoD loop (like #90/#92) — THEN build E-PR6's admin controllers/routes/tests/E2E and remove the inert v1 buttons. This is a multi-repo effort larger than E-PR4/E-PR5.
    - **Precise core-seam implementation notes (from a 2026-07-17 code dive, to make each core PR fast):**
      1. **approve/reject-by-hash** (the invasive one): `FlowEngine::resume($token)`/`reject($token)` → `decideApproval($token,…)`, which threads the PLAIN `$token` through `approvalDecisionRecord($token)` → `ApprovalTokenManager::find($token)` (hashes internally via `hashToken`), `withApprovalDecisionLock($token,…)`, and `consumeApprovalDecisionForPausedRun($token,…)` → `ApprovalTokenManager::consume($token)` (hashes again). EVERY use of `$token` immediately hashes it. Cleanest refactor: compute `$tokenHash` once and thread the HASH (not the plain token) through the whole `decideApproval` private chain + add `ApprovalTokenManager::consumeByHash($tokenHash,…)` / `findByHash($tokenHash)`; then expose `FlowEngine::resumeByHash($tokenHash,…)` / `rejectByHash($tokenHash,…)` + `Flow::resumeByHash`/`rejectByHash` `@api`. Risk: this is the approval-decision critical path (cache lock, run continuation, graph-vs-linear engines) — needs the full existing approval test matrix re-run plus new by-hash tests. Do this one FIRST and in isolation.
      2. **cancel-run**: no core method exists. Add `FlowEngine::cancel(string $runId, array $actor = [])` + `Flow::cancel` `@api` that transitions a non-terminal run (and its `pending`/`running` nodes) to a cancelled terminal state via the run/run-node repositories, guarding against cancelling an already-terminal run.
      3. **replay-run**: no core method. Add `FlowEngine::replay(string $runId, ?FlowExecutionOptions $options = null): FlowRun` + `Flow::replay` `@api` — re-`execute()` the original run's definition with its recorded input, returning a NEW run id (a "linked run"; the admin shows the linkage). Reuse `FlowDashboardReadModel::findRun()` for the source input, or read the run record directly in core.
      4. **retry-webhook-by-id**: `EloquentWebhookOutboxRepository` is `@internal` and only has `claimNextPending(now, timeout)` + `markDeliveryResult(...)`. Add an `@api` seam — e.g. a `Contracts\WebhookOutboxRepository` interface (or a `Flow::redeliverWebhook(int $outboxId)` facade method) that resets ONE failed row by id back to `pending` (so `flow:deliver-webhooks` / the delivery path re-attempts it), or delivers it synchronously via `WebhookDeliveryClient`. Least important of the four.
    - **E-PR6 admin side (after the core seams land)**: controllers wrapping each in `Support\Authorize::action(...)` (the action map already exists), routes (`POST /approvals/{tokenHash}/approve|reject`, `POST /runs/{id}/cancel|replay`, `POST /outbox/{id}/redeliver`), Feature tests (each 403 by default + succeeds with an allowing authorizer), Playwright happy paths, and DELETE every inert/fake v1 button in the blades. Approvals identify by **token hash** (the admin's `Approval` DTO / `ApprovalsController` already surface a hash-shaped id) — never a plain token.
  - After E-PR6: E-PR7 (dry-run viz + cost — core DOES expose `DryRunPlanner::plan()`/`Flow::dryRunGraph()`, so likely no core PR needed), E-PR8 (advisor + AI builder UI, needs `laravel-flow-ai` dep + its `FlowBuilderService`/`FlowAdvisor` HTTP wiring), Macro E Gate G3 (merge `task/v2e-studio` → `main`), Macro G (release + knowhow consolidation across all packages).


- **Now (2026-07-17)**: **E-PR3 MERGED** (PR #36 squash-merged into `task/v2e-studio` as `8a51243`, branch deleted). All 6 CI checks green (PHP 8.3/8.4, Frontend, E2E chromium/firefox/webkit), all 9 Copilot/Codex review threads resolved. **Next: E-PR4 (Versioning UI)** — publish (immutability confirmation modal) + version list + visual diff, on a subtask branch off `task/v2e-studio`. Remaining Macro E: E-PR4→E-PR8, then Gate G3, then Macro G (release). E-PR5 is gated on a small core PR adding `cache_hit` to `Dashboard\StepSummary`.
- **E-PR3 outcome (the hard part was CI, not the feature)**: the editor itself (palette, drag&drop, typed-connection validation, inspector, save-as-draft, node delete with edge pruning) landed cleanly; the multi-round fight was the E2E "save as draft" 500 that was green locally, red in CI. Root cause (confirmed only after adding a CI step that dumps the testbench `laravel.log` on failure): the `testbench serve` app wrote to testbench's default `testing`/`:memory:` connection — empty per request → `no such table: flow_definitions` — because `artisan serve` runs the `php -S` worker with CWD = the skeleton's `public_path()` and drops non-passthrough env, so the serve script's process-env `DB_*` never reached it. Fix: force `DB_CONNECTION: sqlite` in `testbench.yaml` and migrate the skeleton's OWN `database_path('database.sqlite')` (both processes boot the `@testbench` skeleton → identical absolute path). WAL journal mode + a `dragConnect` boundingBox guard + a `.toPass()` retry on the flaky react-flow node-selection click also landed. Two false starts along the way (a WAL-lock hypothesis, then a relative-`DB_DATABASE` path that resolves against the wrong CWD) were both caught by local Copilot review before merge. See `docs/LESSON.md` 2026-07-16 entry.

<!-- superseded detail below retained for history -->
- **(historical) 2026-07-16**: **E-PR3 (canvas editor) PR #36** (`task/v2e-03-canvas-editor` → `task/v2e-studio`) — fixing red CI E2E jobs. **Confirmed root cause via a new "dump laravel.log on E2E failure" CI step**: the `testbench serve` app's save-as-draft write ran against testbench's default `testing`/`:memory:` connection (empty → `no such table: flow_definitions` → 500), NOT the persistent SQLite file the serve script migrated — because the `php -S` grandchild did not inherit the serve script's process env on CI's Ubuntu runner (green locally, where it did). **Fix**: force `DB_CONNECTION: sqlite` in `testbench.yaml`'s `env:` block (read by the served app itself; does NOT leak into the phpunit suite), and migrate the skeleton's OWN `database_path('database.sqlite')` from `serve-testbench.mjs` — both the migrate process and the served app boot the `@testbench` skeleton so both compute the identical absolute path via the connection's `env('DB_DATABASE', database_path(...))` default, with no relative-path/CWD/env-inheritance dependency (a first attempt using a relative `DB_DATABASE` in testbench.yaml was WRONG — `artisan serve` runs the `php -S` worker with CWD = the skeleton's `public_path()`, caught by local Copilot review round 7). Verified by inspecting the skeleton `database/database.sqlite` after an E2E run: the persisted `OrderCheckoutFlow`/`studio-e2e-delete-node-regression` draft rows prove the served app wrote to that exact absolute path. An earlier WAL-journal hypothesis (`scripts/enable-wal.php`) was wrong-but-kept as genuine defense now that writes actually hit the shared file concurrently with the `/flow/api/live` poll reads. Also addressed all 4 PR-review comments (3 Copilot + 1 Codex P2): tempnam leak in `MigratesFlowTables`, undefined `--border-default`/`--text-primary` CSS tokens (→ `--border`/`--text`), guaranteed test DB cleanup via `tearDown()`, `recomputeEdgeValidity()` so a fan-in-invalid wire recovers after its conflicting edge is deleted. Local Copilot review NO_FINDINGS on the round-6 code (round 6a caught a stray NUL byte in a fan-in key, fixed).
- **Local gate (2026-07-16, PHP 8.5 fresh server)**: Pint ✅, PHPStan level 8 ✅, PHPUnit ✅ (147 tests / 769 assertions, unaffected by testbench.yaml DB env), ESLint ✅, build ✅, Playwright studio-editor chromium 7/7. **Next remote step**: push the connection fix, re-verify CI E2E green on PR #36 head (this is the real test — local always passed because it inherited the env), re-request Copilot review, resolve comments, merge into `task/v2e-studio`. Earlier: **E-PR0 DONE** (PR #30 + docs #31), **E-PR1 DONE** (PR #33), **E-PR2 DONE** (PR #35) — see "Macro E" section below.
- **Blocked**: none.

## Macro E — Flow Studio UI (in progress)

- **E-PR1 DONE** (2026-07-14): `task/v2e-01-react-island` merged into `task/v2e-studio` via PR #33 (squash, commit `0d59d0d`), 5 rounds of local Copilot CLI review + 3 rounds of PR-level review (2 CI-only iterations fixing real bugs the review didn't need to catch, 1 substantive Copilot/Codex round), all converged to zero outstanding findings. Delivered: `react`/`react-dom`/`@xyflow/react` dependencies; `resources/js/studio.jsx` (a React island proving the build/serve/mount pipeline with an empty `ReactFlowProvider`/`ReactFlow` canvas); `GET /flow/studio` page + sidebar nav entry; `AbstractManifestAssetController` (resolves Vite's `.vite/manifest.json` to serve the current content-hashed build output through package-internal routes, since Testbench's `serve` can't expose this package's own `public/vendor/flow-admin/` through its public dir) with a path-traversal containment check and RFC-7232-correct 304 semantics; `@stack('head')`/`@stack('scripts')` on the layout. **3 real CI bugs found and fixed post-review** (all recorded in `docs/LESSON.md` 2026-07-14): (1) `defaults.run.working-directory` doesn't apply to `uses:` steps — a `download-artifact` path bug; (2) a "build once instead of 3x" fix that was cosmetic (moved the build call, didn't dedupe it — real fix needed cross-job `upload-artifact`/`download-artifact`); (3) `upload-artifact`'s default `include-hidden-files: false` silently dropped `.vite/manifest.json`, breaking the Studio E2E test specifically while every other page kept passing. A stale `package-lock.json` (missing two optional transitive deps due to registry-state drift, unrelated to any `package.json` change) also broke CI's `npm ci` once and was fixed by a clean regenerate.
- **E-PR0 DONE** (2026-07-14): `task/v2e-00-hygiene` merged into `task/v2e-studio` via PR #30 (squash, commit `d4c63f5`) + docs follow-up PR #31 (squash, commit `2751815`), 6 rounds of local Copilot CLI review + 2 rounds of PR-level review, all converged to zero outstanding findings. Delivered: `composer.json` retargeted to core `dev-main` via a local `path` repository (core has no v2 tag yet); `EloquentReadModel` rewritten to route every read through core's `@api` `Dashboard\FlowDashboardReadModel` + `Contracts\DefinitionRepository` (zero raw `DB::table('flow_*')` calls); a real pre-existing bug fixed (declared step count was reading step-execution rows, not the definition's declared graph node count); plain listing/single-status filtering on runs/approvals/outbox now uses true unbounded server-side pagination (only free-text search — or, for runs, the compound `'failed'` status / a flow-prefix filter — still scans the 200-most-recent-runs bound); a real N+1 eliminated on the runs list (fixed via a small companion core PR, `padosoft/laravel-flow#90`, adding `FlowDashboardReadModel::stepCounts()`); the Claude Design "Flow Studio UI" template copied into `design/claude-design-template/`; CI's `php`/`e2e` jobs updated to checkout core as a true sibling directory.

### Prior state (pre-Macro-E, archived)

- Roadmap implementation merged to `main` via macro PR #20 (`task/read-model-adapter` → `main`, merge SHA `2c80d11`); validated locally on 2026-05-06 at 101 tests / 584 assertions, `npm run test:e2e` 18 passed / 3 visual-gated skipped; PR #19 (subtask) and PR #20 (macro) both merged with all required CI checks green. Publishing release tags was the next step before Macro E's `dev-main` retarget superseded it (`padosoft/laravel-flow` has no v2 tag yet, so a new admin release waits for that).

## Macro 2 — DONE ✅

Squash-merged onto `main` at SHA `1f5d0ed` (macro PR #11).

| PR | Type | Squash SHA |
|----|------|------------|
| #9 | subtask 2.1+2.2 (composer / phpunit / pint / phpstan / ServiceProvider skeleton + 13 tests) | `c83b7d9` on `task/baseline-tooling` |
| #10 | subtask 2.3 (Vite + Alpine + ESLint 9 + Playwright 1.59 + testbench.yaml + 23 tests / 34 assertions) | `34d3ee0` on `task/baseline-tooling` |
| #12 | subtask review hotfix (7 Copilot threads on macro PR) | `c2d6ed1` on `task/baseline-tooling` |
| #11 | macro → main | `1f5d0ed` on `main` |

## Macro 1 — DONE ✅

Squash-merged onto `main` at SHA `f32ac2f` (macro PR #2).

| PR | Type | Squash SHA |
|----|------|------------|
| #1 | subtask (rules/skills/agents/docs/CI scaffold) | `2c6f478` on `task/agent-operating-system` |
| #3 | subtask hotfix (design-path fix in 7 entrypoints) | `e9c8194` on `task/agent-operating-system` |
| #4 | subtask hotfix (6 genuine Copilot issues: PROGRESS/CI/plan/PII/README) | `d4a2e49` on `task/agent-operating-system` |
| #2 | macro → main | `f32ac2f` on `main` |

## Active branches

| Branch | Base | Status |
|--------|------|--------|
| `main` | n/a | active release branch |
| `task/v2e-studio` | `main` | **ACTIVE — Macro E (Flow Studio UI)**. E-PR0 merged (PR #30 + docs follow-up PR #31); E-PR1 merged (PR #33 + docs follow-up PR #34). Next subtask: E-PR2 (read-only canvas rendering), branch off `task/v2e-studio`, e.g. `task/v2e-02-canvas-readonly`. |
| `task/read-model-adapter` | `main` | merged via PR #20 |
| `subtask/read-model-2-eloquent` | `task/read-model-adapter` | merged via PR #19 |

## Macro task status

| # | Macro | Branch | State |
|---|-------|--------|-------|
| 1 | Agent Operating System | `task/agent-operating-system` | ✅ merged on main `f32ac2f` |
| 2 | Baseline Tooling Laravel 13 | `task/baseline-tooling` | ✅ merged on main `1f5d0ed` |
| 3 | Design System & Layout Shell | `task/design-system-shell` | ✅ merged on main `617e427` |
| 4 | Read Model Adapter | `task/read-model-adapter` | ✅ merged on main `2c80d11` |
| 5 | Pages — Overview & Runs | `task/pages-overview-runs` | ✅ delivered in `2c80d11` |
| 6 | Pages — Run Detail | `task/pages-run-detail` | ✅ delivered in `2c80d11` |
| 7 | Pages — Approvals/Outbox/Definitions/Settings | `task/pages-misc` | ✅ delivered in `2c80d11` |
| 8 | ⌘K Palette + Auto-refresh + Toasts | `task/cmdk-search` | ✅ delivered in `2c80d11` |
| 9 | Hardening, README, Release | `task/hardening-release` | ✅ docs/release artifacts merged |
| 10 | Harvest LESSON.md → rules/skills | `task/lessons-harvest` | ✅ lesson harvest merged |

## Restart steps

If you re-enter this repo from a cold start:

1. Open `docs/IMPLEMENTATION_PLAN.md` — confirm the canonical plan is unchanged.
2. Open this file (`docs/PROGRESS.md`) — find the Active branches table.
3. `git fetch --all && git switch <branch from table>`.
4. `git status` — confirm clean working tree.
5. If `composer.json` exists: `composer install`. If `package.json` exists: `npm ci`. (Not present yet during Macro 1 scaffold — skip if absent.)
6. Run the relevant local gates (skip commands for tools not yet present): `composer validate --strict --no-check-publish && composer format:test && composer analyse && composer test` (when `composer.json` exists) + `npm run lint && npm run build && npm run test:e2e` (when `package.json` and `playwright.config.js` exist).
7. If the next subtask is implementation: read the corresponding §3 entry in `IMPLEMENTATION_PLAN.md`.
8. If you are mid-PR loop: run `gh pr view <N> --json state,reviewDecision,statusCheckRollup` and continue the Copilot+CI loop per `.claude/skills/copilot-pr-review-loop/SKILL.md`.
